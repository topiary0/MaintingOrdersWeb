@model IEnumerable<MaintainingOrdersWeb.Models.SupplyItem>

@{
    ViewData["Title"] = "Составы поставок";
}

<h1 style="color: #1976D2;">Составы поставок</h1>

@if (User.IsInRole("Директор") || User.IsInRole("Менеджер"))
{
    <p>
        <a asp-action="Create" class="btn btn-success" style="background-color: #2E7D32;">Добавить позицию</a>
    </p>
}
<table class="table table-modern">
    <thead>
        <tr>
            <th>Поставка</th>
            <th>Товар</th>
            <th>Количество</th>
            <th>Цена в поставке</th>
            <th></th>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model)
        {
            <tr>
                <td>@Html.DisplayFor(modelItem => item.ShipmentId)</td>
                <td>@Html.DisplayFor(modelItem => item.Product.Name)</td>
                <td>@Html.DisplayFor(modelItem => item.Quantity)</td>
                <td>@Html.DisplayFor(modelItem => item.PriceAtShipment)</td>
                <td>
                    @if (User.IsInRole("Директор") || User.IsInRole("Менеджер"))
                    {
                        <a asp-action="Edit" asp-route-shipmentId="@item.ShipmentId" asp-route-productId="@item.ProductId" class="btn btn-outline-primary btn-sm">Редактировать</a>
                        <a asp-action="Details" asp-route-shipmentId="@item.ShipmentId" asp-route-productId="@item.ProductId" class="btn btn-outline-info btn-sm">Детали</a>
                        <a asp-action="Delete" asp-route-shipmentId="@item.ShipmentId" asp-route-productId="@item.ProductId" class="btn btn-outline-danger btn-sm">Удалить</a>
                    }
                    else
                    {
                        <a asp-action="Details" asp-route-shipmentId="@item.ShipmentId" asp-route-productId="@item.ProductId" class="btn btn-outline-info btn-sm">Детали</a>
                    }
                </td>
            </tr>
        }
    </tbody>
</table>