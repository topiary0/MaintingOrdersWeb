@model IEnumerable<MaintainingOrdersWeb.Models.OrderStatus>

@{
    ViewData["Title"] = "Статусы заказов";
}

<h1 style="color: #1976D2;">Статусы заказов</h1>

@if (User.IsInRole("Директор") || User.IsInRole("Менеджер"))
{
    <p>
        <a asp-action="Create" class="btn btn-success" style="background-color: #2E7D32;">Создать новый статус</a>
    </p>
}
<table class="table table-modern">
    <thead>
        <tr>
            <th>Название</th>
            <th></th>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model)
        {
            <tr>
                <td>@Html.DisplayFor(modelItem => item.StatusName)</td>
                <td>
                    @if (User.IsInRole("Директор") || User.IsInRole("Менеджер"))
                    {
                        <a asp-action="Edit" asp-route-id="@item.StatusId" class="btn btn-outline-primary btn-sm">Редактировать</a>
                        <a asp-action="Details" asp-route-id="@item.StatusId" class="btn btn-outline-info btn-sm">Детали</a>
                        <a asp-action="Delete" asp-route-id="@item.StatusId" class="btn btn-outline-danger btn-sm">Удалить</a>
                    }
                    else
                    {
                        <a asp-action="Details" asp-route-id="@item.StatusId" class="btn btn-outline-info btn-sm">Детали</a>
                    }
                </td>
            </tr>
        }
    </tbody>
</table>