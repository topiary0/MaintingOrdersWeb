@model IEnumerable<MaintainingOrdersWeb.Models.DeliveryMethod>

@{
    ViewData["Title"] = "Способы доставки";
}

<h1 style="color: #1976D2;">Способы доставки</h1>

@if (User.IsInRole("Директор"))
{
    <p>
        <a asp-action="Create" class="btn btn-success" style="background-color: #2E7D32;">Создать новый способ</a>
    </p>
}
<table class="table">
    <thead>
        <tr>
            <th>Название</th>
            <th>Цена</th>
            <th>Примерное время</th>
            <th></th>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model)
        {
            <tr>
                <td>@Html.DisplayFor(modelItem => item.MethodName)</td>
                <td>@Html.DisplayFor(modelItem => item.Price)</td>
                <td>@Html.DisplayFor(modelItem => item.EstimatedTime)</td>
                <td>
                    @if (User.IsInRole("Директор"))
                    {
                        <a asp-action="Edit" asp-route-id="@item.MethodId" style="color: #1976D2;">Редактировать</a> @:|
                        <a asp-action="Delete" asp-route-id="@item.MethodId" style="color: #FF6D00;">Удалить</a> @:|
                    }
                    <a asp-action="Details" asp-route-id="@item.MethodId" style="color: #1976D2;">Детали</a>
                </td>
            </tr>
        }
    </tbody>
</table>