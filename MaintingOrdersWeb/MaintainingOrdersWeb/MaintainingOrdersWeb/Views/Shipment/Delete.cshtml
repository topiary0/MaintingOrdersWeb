@model MaintainingOrdersWeb.Models.Shipment

@{
    var canDelete = ViewBag.CanDelete as bool? ?? true;
    ViewData["Title"] = "Удаление поставки";
}

<h1 class="form-page-title">Удаление поставки</h1>

<h3 class="delete-confirm-text">Вы уверены, что хотите удалить эту поставку?</h3>
<div>
    <partial name="_DeleteDependencyWarning" />
    <dl class="row">
        <dt class="col-sm-2">Дата поставки</dt>
        <dd class="col-sm-10">@Html.DisplayFor(model => model.ShipmentDate)</dd>
        <dt class="col-sm-2">Статус (текст)</dt>
        <dd class="col-sm-10">@Html.DisplayFor(model => model.Status)</dd>
        <dt class="col-sm-2">Поставщик</dt>
        <dd class="col-sm-10">@Html.DisplayFor(model => model.Suppliers.SupplierName)</dd>
        <dt class="col-sm-2">Менеджер</dt>
        <dd class="col-sm-10">@Html.DisplayFor(model => model.User.FullName)</dd>
        <dt class="col-sm-2">Статус поставки</dt>
        <dd class="col-sm-10">@Html.DisplayFor(model => model.Statussh.StatusName)</dd>
    </dl>

    <form asp-action="Delete" class="delete-form">
        <input type="hidden" asp-for="ShipmentId" />
        @if (canDelete)
        {
            <input type="submit" value="Удалить" class="btn btn-danger delete-submit-btn btn-uniform" />
        }
        else
        {
            <button type="button" class="btn btn-warning delete-submit-btn btn-uniform" disabled>Удаление недоступно</button>
        }
        <a asp-action="Index" class="btn btn-secondary btn-uniform">Отмена</a>
    </form>
</div>